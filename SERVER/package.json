{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "test.lesson.js",
  "scripts": {
    "build": "npx tsc",
    "start": "rm -rf ./build && npm run build && node dist/test.lesson.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.1.3",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "fs.promises": "^0.1.2",
    "sweetalert": "^2.1.2",
    "sweetalert2": "^11.4.18"
  },
  "devDependencies": {
    "typescript": "^4.7.4"
  }
}
